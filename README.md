# hayagreev-medha-labs-templates
Web Application Templates

## Templates

- `spring-boot-user-api/` — Spring Boot (Maven) REST API template with PostgreSQL + JPA/Hibernate, CRUD endpoints for a `User` entity, and JUnit tests.
